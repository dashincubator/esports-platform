[client]
socket = ${MYSQL_SOCKET}

[mysqld]
character-set-server = utf8mb4
collation-server = utf8mb4_general_ci
skip-character-set-client-handshake
skip-networking
socket = ${MYSQL_SOCKET}

[mysqld_safe]
socket = ${MYSQL_SOCKET}
